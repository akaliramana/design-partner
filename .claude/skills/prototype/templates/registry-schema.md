# Registry Schema Template

Use this as the starting point when creating a new `.ui-skill/registry.json`:

```json
{
  "version": "1.0",
  "components": {}
}
```

Add components as they are created. See `references/component-registry.md` for the full schema.
